# OpenCodex 缓存改造移交文档

> 接手者目标:在**有 Redis 的环境**继续验证、上线、并完成剩余工作。本文档自包含,无需回看会话历史。

## 一、项目背景

OpenCodex 是 .NET 10 / ASP.NET Core 的协议中转代理(EF Core + SQLite/PostgreSQL)。本次改造为其加入两级缓存(L1 进程内内存 + L2 Redis)与跨实例共享状态,参考了同机另一个项目 `shl_file_server` 的 Redis 用法(StackExchange.Redis 双检锁单例 + `WithKeyPrefix`)。

差异点:OpenCodex 用内置 DI(非 Autofac)、配置走 `OPENCODEX_*` 环境变量 + `OpenCodex:配置节`(非 appsettings.json 的 `RedisConfig`)。

## 二、启用 Redis(首要操作)

### 2.1 环境变量

| 变量 | 说明 | 默认 |
|---|---|---|
| `OPENCODEX_REDIS_CONNECTION` | Redis 连接串。**为空 = 禁用 Redis**,缓存与共享状态降级为纯进程内,服务照常启动 | 空(禁用) |
| `OPENCODEX_REDIS_PREFIX` | 全局 key 前缀,实现在挂载时追加 `:` | `opencodex` |
| `OPENCODEX_CACHE_DEFAULT_TTL_SECONDS` | L1/L2 默认 TTL | `300` |

读取位置:`Presentation/OpenCodex.Api/Configuration/OpenCodexRuntimeSettingsProvider.cs`,挂在 `OpenCodexRuntimeSettings` 上。

连接串示例(参考 shl_file_server,带密码):
```
OPENCODEX_REDIS_CONNECTION=192.168.5.9:6379,abortConnect=false,password=148155
```
**必须带 `abortConnect=false`**(代码侧也强制 `AbortOnConnectFail=false`),否则 Redis 不通时服务启动会抛异常——这与"可选降级"设计相悖。

### 2.2 待办:`.env.example` 未补 Redis 配置

当前 `.env.example` **没有** Redis 配置项。需补一段(建议加在数据库配置后):
```env
# ===== Redis 缓存与共享状态(可选,留空则降级为纯进程内) =====
# 为空时禁用 Redis;启用后鉴权/路由/定价缓存走两级,亲和/容量走跨实例共享
OPENCODEX_REDIS_CONNECTION=
OPENCODEX_REDIS_PREFIX=opencodex
OPENCODEX_CACHE_DEFAULT_TTL_SECONDS=300
```

## 三、已完成清单

### U0 缓存基础设施(纯新增)

两级缓存门面 + Redis 连接 + Pub/Sub 跨实例失效广播。

| 文件 | 作用 |
|---|---|
| `Libraries/OpenCodex.CoreBase/Caching/ICacheService.cs` | 门面抽象:`GetOrCreateAsync<T>(key, factory, ttl?)` / `RemoveAsync(key)` / `RemoveAsync(keys)`,纯 BCL 签名 |
| `Libraries/OpenCodex.CoreBase/Caching/CacheKeys.cs` | 集中管理缓存键格式,读/失效两侧共用 |
| `Libraries/OpenCodex.Core/Services/Caching/IRedisConnectionProvider.cs` | Redis 连接抽象:`IsAvailable` / `KeyPrefix` / `GetDatabase()` / `GetSubscriber()` |
| `Libraries/OpenCodex.Core/Services/Caching/RedisConnectionProvider.cs` | 双检锁单例 `ConnectionMultiplexer`,`AbortOnConnectFail=false`、`ConnectRetry=3`、`ExponentialRetry(5000)`;`GetDatabase()` 用 `WithKeyPrefix` 统一挂前缀;连不上 `IsAvailable=false` |
| `Libraries/OpenCodex.Core/Services/Caching/TwoLevelCacheService.cs` | L1(`IMemoryCache`)→L2(Redis,`System.Text.Json`)→回源,逐层写回;`RemoveAsync` 删 L1+删 L2+发 Pub/Sub;通道 `{prefix}:cache-invalidation`,消息 `{instanceId}|{key}` |

DI 注册在 `Presentation/OpenCodex.Api/Hosting/OpenCodexServiceCollectionExtensions.cs`:`AddMemoryCache()` + `IRedisConnectionProvider` 单例 + `ICacheService` 单例。

### U1 鉴权缓存(热路径,最高收益)

- **读**:`Proxy/ProxyAccessService.cs` — 每个 Bearer 请求按 `KeyHash` 查 AccessApiKey + 查 User,走两级缓存
- **失效**:`ApiKeyService.cs`(Update/Delete/Import 删 `auth:apikey:{hash}`)、`UserService.cs`(停用/改密/删除删 `auth:user:{userId}`)
- **Key**:`CacheKeys.AuthApiKey(hash)` = `auth:apikey:{hash}`、`CacheKeys.AuthUser(userId)` = `auth:user:{userId}`
- 鉴权接口已 async 化(方案 B),级联:`IProxyAccessService` → `IProxyRequestService` → `ProxyEndpointService.ProxyAsync` → `ProxyController`

### U2 渠道配置缓存

- **读**:`Proxy/ProxyRouteService.cs` — `route:channels:{ownerUsername}` 缓存该用户启用渠道实体集
- **失效**:`ConfigService.cs` — 渠道增删改/重排后失效
- **Key**:`CacheKeys.RouteChannels(ownerUsername)` = `route:channels:{ownerUsername}`

### U3 模型目录 + 定价上下文缓存

- **读**:`ModelCatalogService.cs` — 目录列表 + 定价上下文(`ModelCatalogService.cs:500` 用 `CacheKeys.PricingContext`)
- **Key**:`CacheKeys.PricingContext(channelId, upstreamModel)` = `pricing:context:{channelId}:{upstreamModel}`
- 注:`ModelPricingService.cs` 管理定价规则(写路径),未直接引用 `ICacheService`;定价上下文缓存的失效点在 `ModelCatalogService` / 定价规则变更路径,接手后请核实失效是否覆盖规则编辑(见第五节待验证)

### U4 跨实例共享状态(已做 Affinity + Capacity)

三个原本进程内(Singleton + `ConcurrentDictionary`)的运行时状态服务,在 Redis 可用时上 Redis 共享;不可用降级回进程内。`IRedisConnectionProvider` 作**可选 ctor 参数**(默认 null → 内存降级),DI 注入真实 provider,测试无参构造仍走内存。

| 服务 | Redis key | 机制 | 文件 |
|---|---|---|---|
| 亲和映射 | `affinity:{owner}:{stickyKey}` | `Remember`=`SET EX`,`Get`=`GET+EXPIRE`(滑动) | `Proxy/ChannelAffinityService.cs` |
| 容量计数 | `capacity:{owner}:{channelId}` | Sorted Set(member=leaseId, score=过期戳);`TryAcquire` 用 **Lua 原子**(清过期+判满+占位);`Release`=`ZREM` fire-and-forget;**租约 TTL 600s** 防崩溃泄漏 | `Proxy/ChannelCapacityService.cs` |

两个服务接口 async 化:`IChannelAffinityService`(`GetPreferredChannelIdAsync`/`RememberAsync`)、`IChannelCapacityService`(`TryAcquireAsync`)。调用点全在 `ProxyEndpointService`(已 async),级联可控。

**重要取舍**:`GetActiveRequests` / `GetActiveModelUsages` **保持同步 + 进程内**(不查 Redis),用于最小连接排序启发式与管理台展示。多实例下为本实例视角近似;**全局硬限流以 Redis 为准**(不受影响)。这样避免热路径每候选 N 次 Redis 往返。

### 测试状态

`OpenCodex.Api.Tests` 共 287 个测试全部通过(走内存降级路径,即 Redis 未启用时的行为)。Redis 路径运行时未测(见第五节)。

## 四、缓存架构速览

```
读路径: L1 内存(IMemoryCache) → L2 Redis(System.Text.Json) → 回源 DB,逐层写回
写路径: 改 DB 后 → 删本地 L1 + 删 Redis L2 + Pub/Sub 广播 → 其它实例收到后删各自 L1
降级:   Redis 不可用(IsAvailable=false)→ 纯 L1 + TTL,服务照常
```

- 序列化:`System.Text.Json`(无二进制)
- 失效策略:写穿失效(write-through invalidation)+ TTL 兜底
- 前缀:业务代码写裸逻辑 key,`RedisConnectionProvider.GetDatabase()` 用 `WithKeyPrefix` 统一挂 `{prefix}:`

## 五、待验证清单(部署 Redis 后必须测)

> 沙箱无 Redis,以下仅编译通过,运行时正确性未验证。

1. **Lua + WithKeyPrefix 前缀隔离**:`ChannelCapacityService` 的 Lua 脚本里 `@key` 是否被 `WithKeyPrefix` 正确加前缀。若不加,key 仍可用但失去环境隔离(实际因 `capacity:`/`affinity:` 前缀不与他服务冲突,功能不受影响,但需确认)
2. **鉴权缓存读写 + 失效**:登录→请求命中缓存;改 apikey/停用用户后缓存及时失效,不返回脏数据
3. **渠道缓存失效**:增删改渠道后 `route:channels:{owner}` 被清,路由读到新配置
4. **定价上下文失效**:编辑定价规则后 `pricing:context:*` 被清,**重点核实 `ModelPricingService` 规则编辑路径是否触发了失效**(U3 失效点未完全确认)
5. **亲和映射跨实例**:实例 A `Remember` 后,实例 B `Get` 能读到(验证 Redis 共享生效,而非各自内存)
6. **容量信号量**:capacity=2 时第 3 个 `TryAcquire` 返回 null;`Release` 后可重新获取;**模拟实例崩溃(kill 进程不 Release)→ 600s 内槽位自动回收**
7. **Pub/Sub 跨实例失效**:实例 A 改 apikey → 实例 B 的 L1 被清(验证多实例 L1 一致性)
8. **降级**:Redis 连不上时,所有缓存与共享状态回退进程内,服务不报错

## 六、待办

| 项 | 说明 | 优先级 |
|---|---|---|
| **U4c 熔断上 Redis** | `ChannelCircuitBreakerService` 仍进程内。独立-per-instance 不破坏正确性(每实例自保),多实例下全实例封禁需 N×threshold 次失败。若需协调熔断,需 5-6 个 Lua 脚本保证状态转换(Open→HalfOpen→Closed + 计数)原子 | 低(可选) |
| **补 `.env.example`** | 加 Redis 配置段(见 2.2) | 高 |
| **运行时验证** | 第五节全部 8 项 | 高 |
| **租约 TTL 调优** | `ChannelCapacityService.LeaseTtl = 600s`,若有超长流式请求可调大;但调大后崩溃回收变慢 | 按需 |
| **容量信号量压力测试** | 高并发下 Lua 原子性、TTL 回收时序 | 按需 |

## 七、关键文件索引

```
缓存基础设施(U0):
  src/Libraries/OpenCodex.CoreBase/Caching/ICacheService.cs
  src/Libraries/OpenCodex.CoreBase/Caching/CacheKeys.cs
  src/Libraries/OpenCodex.Core/Services/Caching/IRedisConnectionProvider.cs
  src/Libraries/OpenCodex.Core/Services/Caching/RedisConnectionProvider.cs
  src/Libraries/OpenCodex.Core/Services/Caching/TwoLevelCacheService.cs

缓存接入点(U1-U3):
  src/Libraries/OpenCodex.Core/Services/Proxy/ProxyAccessService.cs     # 鉴权读
  src/Libraries/OpenCodex.Core/Services/ApiKeyService.cs               # apikey 失效
  src/Libraries/OpenCodex.Core/Services/UserService.cs                 # user 失效
  src/Libraries/OpenCodex.Core/Services/Proxy/ProxyRouteService.cs     # 渠道路由读
  src/Libraries/OpenCodex.Core/Services/ConfigService.cs               # 渠道失效
  src/Libraries/OpenCodex.Core/Services/ModelCatalogService.cs         # 目录+定价上下文读

共享状态(U4):
  src/Libraries/OpenCodex.Core/Services/Proxy/ChannelAffinityService.cs
  src/Libraries/OpenCodex.Core/Services/Proxy/ChannelCapacityService.cs
  src/Libraries/OpenCodex.CoreBase/Services/Proxy/IChannelAffinityService.cs
  src/Libraries/OpenCodex.CoreBase/Services/Proxy/IChannelCapacityService.cs

配置与 DI:
  src/Libraries/OpenCodex.CoreBase/Abstractions/OpenCodexRuntimeSettings.cs
  src/Presentation/OpenCodex.Api/Configuration/OpenCodexRuntimeSettingsProvider.cs
  src/Presentation/OpenCodex.Api/Hosting/OpenCodexServiceCollectionExtensions.cs

熔断(未做 U4c,仍进程内):
  src/Libraries/OpenCodex.Core/Services/Proxy/ChannelCircuitBreakerService.cs
```

## 八、已知风险与取舍

1. **每请求 Redis 往返**:鉴权(命中时纯内存,未命中回源)、容量(Lua acquire + fire-and-forget ZREM)、亲和(仅 stickyKey 请求,GET+EXPIRE)。本地 Redis 约增 1-2ms/请求。远程 Redis 需评估
2. **GetActiveRequests 跨实例近似**:多实例下最小连接排序基于本实例负载;全局硬限流不受影响
3. **fire-and-forget Release**:ZREM 丢失时靠租约 TTL 回收(最多 600s 占用),可接受
4. **Redis 路径运行时未测**:Lua 脚本、WithKeyPrefix、Sorted Set 信号量、Pub/Sub 的运行时正确性待部署验证(沙箱仅保证编译)
5. **单实例下进程内更优**:若实际只跑单实例,Redis 带来的网络往返是纯开销;但降级设计保证不设 `OPENCODEX_REDIS_CONNECTION` 即等同改造前行为

## 九、快速验证步骤(部署后)

```bash
# 1. 配 .env(补 Redis 连接串)
echo 'OPENCODEX_REDIS_CONNECTION=你的redis:6379,abortConnect=false,password=xxx' >> .env
echo 'OPENCODEX_REDIS_PREFIX=opencodex' >> .env

# 2. 启动,观察日志无 Redis 连接异常
dotnet run --launch-profile OpenCodex.Api --project opencodex_proxy/src/Presentation/OpenCodex.Api/OpenCodex.Api.csproj

# 3. redis-cli 查 key 是否落库(发几个请求后)
redis-cli -h 你的redis -a xxx KEYS "opencodex:*"
#  期望看到 opencodex:auth:apikey:* / opencodex:route:channels:* / opencodex:capacity:* 等

# 4. 验证鉴权缓存:同一 apikey 连发两次请求,第二次不应再查 DB(看日志/SQL)

# 5. 验证失效:管理台停用某 apikey → 立即用该 key 请求应返回 401(不命中旧缓存)

# 6. 验证容量:把某渠道 capacity 设为 1,并发发 2 个请求,第 2 个应被限流
```

---

**编译/测试状态**:全量 `dotnet build` 通过,`OpenCodex.Api.Tests` 287 测试全过(内存降级路径)。Redis 路径运行时验证待部署环境完成。
